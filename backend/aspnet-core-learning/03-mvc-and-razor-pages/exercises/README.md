### Practice Exercises Folder (`exercises/README.md`)
Create the sub-folder structure `exercises/` inside Module 03 and drop a `README.md` containing this project requirement:

```markdown
# Module 03 Exercises: MVC vs Razor Pages

### Exercise 1: Product Catalog View
* **Goal**: Create a product list display using Razor syntax.
* **Requirements**: 
    * In a sample controller, initialize a mock list of products `List<Product>`.
    * Send the list to the View layer.
    * Use a `@foreach` loop inside the `.cshtml` view file to safely render the names and prices inside an HTML table structure.